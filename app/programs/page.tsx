import Image from 'next/image';
import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { ButtonLink, Container, PageHero, SectionHeading } from '@/components/ui';
import { programs } from '@/lib/content';
export const metadata = { title: 'Programs' };
export default function ProgramsPage() { return <><PageHero eyebrow="What we do" title="Programs for healthier futures" intro="We build practical, inclusive pathways for young people to access care, develop skills, lead research and shape resilient communities." /><section className="section"><Container><SectionHeading eyebrow="Our areas of work" title="From health access to climate action" intro="Our programs are connected by one belief: youth leadership and community knowledge are essential to lasting change." /><div className="program-list">{programs.map((program, i) => <article className="program-row" key={program.slug}><div className="program-number">0{i + 1}</div><div className="program-row-image"><Image src={program.image} alt={program.imageAlt} fill sizes="(max-width: 700px) 100vw, 35vw" /></div><div className="program-row-body"><p className="eyebrow">{program.shortTitle}</p><h2>{program.title}</h2><p>{program.summary}</p><Link className="text-link" href={`/programs/${program.slug}`}>Explore program <ArrowUpRight size={15} /></Link></div></article>)}</div></Container></section><section className="section cta-band"><Container className="cta-inner"><h2>Have a program idea?</h2><ButtonLink href="/contact-us" variant="dark">Talk to our team <ArrowUpRight size={17} /></ButtonLink></Container></section></>; }
