import Image from 'next/image';
import { notFound } from 'next/navigation';
import { ButtonLink, Container, PageHero } from '@/components/ui';
import { projects } from '@/lib/content';
export function generateStaticParams() { return projects.map(({ slug }) => ({ slug })); }
export default function ProjectPage({ params }: { params: { slug: string } }) { const p = projects.find(x => x.slug === params.slug); if (!p) notFound(); return <><PageHero eyebrow={p.category} title={p.title} intro={p.summary} /><section className="section"><Container className="article-layout"><div className="article-image"><Image src={p.image} alt={p.imageAlt} fill sizes="(max-width: 900px) 100vw, 70vw" /></div><article className="rich-text"><p>This project is part of AfroVive Health Foundation&apos;s commitment to community-driven learning and youth leadership. We bring together lived experience, practical skills and evidence to make health systems more responsive.</p><p>Through collaboration with young people, universities, community organizations and global partners, the work turns shared knowledge into action that can be adapted and sustained.</p><h2>What we are learning</h2><p>Inclusive partnerships create better questions, stronger evidence and more useful solutions. We document and share the lessons so they can travel across borders.</p><ButtonLink href="/contact-us">Discuss a partnership</ButtonLink></article></Container></section></>; }
