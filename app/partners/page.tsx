import { ArrowUpRight } from 'lucide-react';
import { ButtonLink, Container, PageHero, SectionHeading } from '@/components/ui';
import { getPartners } from '@/lib/api';
export const metadata = { title: 'Partners' };
export const dynamic = 'force-dynamic';
export default async function PartnersPage() { const partners = await getPartners(); return <><PageHero eyebrow="Work with us" title="Partnerships with purpose" intro="We believe the best solutions are co-designed. Our partnerships connect lived experience, technical knowledge and the resources to act." /><section className="section"><Container><SectionHeading eyebrow="Who we work with" title="Stronger together" />{partners.length > 0 ? <div className="partner-grid">{partners.map((partner, i) => <div className="partner-tile" key={partner.id}><span>0{i + 1}</span><h2>{partner.name}</h2><p>{partner.description || 'Join a growing network committed to youth-led health equity and climate justice.'}</p></div>)}</div> : <div className="empty-state">No partners have been published yet.</div>}</Container></section><section className="section cta-band"><Container className="cta-inner"><h2>Have an idea for collaboration?</h2><ButtonLink href="/contact-us" variant="dark">Start a conversation <ArrowUpRight size={17} /></ButtonLink></Container></section></>; }
